require.config({
    baseUrl: "js",
    paths: {
        // Libs paths
        "jquery": "../vendor/jquery/dist/jquery.min",

        // Module paths
        "modules": "app/modules",
    },
    waitSeconds: 1
});